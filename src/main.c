#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>

void parser(uint16_t opcode) {
  // TODO: Implement whole parser
}

int main() {

  // TODO: Figure out where to initialize all stuff (in the future pthreads and raylib)
  srand(0);

  return 0;
}
